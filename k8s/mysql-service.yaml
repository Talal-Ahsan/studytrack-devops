apiVersion: v1
kind: Service
metadata:
  name: studytrack-mysql-service
spec:
  type: NodePort
  selector:
    app: studytrack-mysql
  ports:
    - name: mysql
      protocol: TCP
      port: 3306
      targetPort: 3306
      nodePort: 30306