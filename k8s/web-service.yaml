apiVersion: v1
kind: Service
metadata:
  name: studytrack-web-service
spec:
  type: NodePort
  selector:
    app: studytrack-web
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30080